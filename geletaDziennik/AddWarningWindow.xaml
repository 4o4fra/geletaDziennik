<Window x:Class="geletaDziennik.AddWarningWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Dodaj uwagę" Height="350" Width="600"
        Background="#2E2E2E">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Dodaj punkty" FontSize="24" Foreground="#FFFFFF" VerticalAlignment="Center" Margin="0,0,0,20"/>
        <TextBox Name="WarningTextBox" Grid.Row="1" Height="40" VerticalAlignment="Bottom" Margin="0,0,0,20" Padding="10"/>
        <Button Name="SubmitPointsButton" Grid.Row="2" Content="Dodaj/Usuń punkty" Click="SubmitPointsButton_Click" Width="120" Height="40" Margin="0,0,0,20" HorizontalAlignment="Left" Background="#007ACC"/>
        <DataGrid Name="WarningsDataGrid" Grid.Row="3" Margin="0,20,0,0" Background="#FFFFFF" Foreground="#000000" AutoGenerateColumns="True" ScrollViewer.CanContentScroll="False"/>
    </Grid>
</Window>